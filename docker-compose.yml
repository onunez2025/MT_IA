services:
  open-webui:
    image: ghcr.io/open-webui/open-webui:main
    container_name: open-webui
    ports:
      - "3000:8080"
    environment:
      - OLLAMA_BASE_URL=http://192.168.42.21:11434
      - WEBUI_SECRET_KEY=secret_key_mt_ia_2026
    volumes:
      - open-webui-data:/app/backend/data
    restart: always

volumes:
  open-webui-data:
